{% import "macros.adoc.peb" %}
= ++{{ title ? title : name }}++

{% if description %}
{{ description }}


{% endif %}
{% for child in contents %}
* xref:./{{ child.path }}{{ child.type == "index" ? "" : ".adoc"  }}[++{{ child.title ? child.title : child.name }}++]
{% endfor %}
