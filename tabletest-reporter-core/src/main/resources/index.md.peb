{% import "macros.md.peb" %}
# {{ title ? title : name }}

{% if description %}

{{ description }}

{% endif %}

{% for child in contents %}
* [{{ child.title ? child.title : child.name }}](./{{ child.path }}{{ child.type == "index" ? "" : ".md"  }})
{% endfor %}
