== ++{{ title }}++

{% if description %}
{{ description }}

{% endif %}
[%header,cols="{{ '1' | replicate(columnCount) | join(',') }}"]
|===
{% for header in headers %}
|{{ renderCell(header) }}

{% endfor %}

{% for row in rows %}
{% for cell in row %}
a|{{ renderCell(cell) }}

{% endfor %}

{% endfor %}
|===
{# MACROS #}
{% macro renderCell(cell) %}
{% if cell.role %}[.{{ cell.role }}]#++{{ cell.value }}++#{% else %}++{{ cell.value }}++{% endif %}
{% endmacro %}
